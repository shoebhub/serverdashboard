global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'proxmox-dashboard'
    static_configs:
      - targets: ['server:4000']
    metrics_path: '/metrics'
    scheme: 'http'
